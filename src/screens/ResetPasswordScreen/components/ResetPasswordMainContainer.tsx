import styled from 'styled-components/native';
import { MainContainer } from '../../../components/Containers/MainContainer';

export const ResetPasswordMainContainer = styled(MainContainer)`
  gap: ${({ theme }) => theme.space[2]}px;
`;
