import styled from 'styled-components/native';

export const PatientListMainContainer = styled.View`
  flex: 1;
  padding: ${({ theme }) => theme.space[4]}px;
  gap: ${({ theme }) => theme.space[2]}px;
`;
